<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= habit.title %> - HabitFlow</title>
    <style>
        body { font-family: 'Inter', sans-serif; background: #050505; color: #ededed; padding: 20px; }
        .container { max-width: 600px; margin: 0 auto; }
        
        .detail-card {
            background: #0f0f0f;
            border: 1px solid #27272a;
            border-radius: 24px;
            padding: 32px;
            text-align: center;
        }

        .category-badge {
            background: #1a1a1a; color: #a1a1aa;
            padding: 4px 12px; border-radius: 100px;
            font-size: 12px; text-transform: uppercase; letter-spacing: 1px;
            display: inline-block; margin-bottom: 16px;
        }

        h1 { margin: 0 0 12px 0; font-size: 32px; }
        p { color: #a1a1aa; line-height: 1.6; margin-bottom: 32px; }

        .stats-row {
            display: flex; justify-content: center; gap: 32px;
            border-top: 1px solid #27272a; border-bottom: 1px solid #27272a;
            padding: 24px 0; margin-bottom: 32px;
        }
        .stat-label { display: block; font-size: 12px; color: #71717a; text-transform: uppercase; }
        .stat-val { font-size: 24px; font-weight: 700; color: #fff; }

        .actions { display: flex; gap: 12px; justify-content: center; }
        .btn {
            padding: 10px 20px; border-radius: 12px; text-decoration: none; font-weight: 500;
            transition: transform 0.2s;
        }
        .btn:hover { transform: translateY(-2px); }
        .btn-primary { background: #ededed; color: #050505; }
        .btn-outline { border: 1px solid #27272a; color: #a1a1aa; }
        .btn-danger { border: 1px solid #ef4444; color: #ef4444; background: transparent; cursor: pointer; font-family: inherit; font-size: 16px; }
    </style>
</head>
<body>

<div class="container">
    <a href="/" style="color:#71717a; text-decoration:none; margin-bottom:20px; display:block;">← Back to Dashboard</a>

    <div class="detail-card">
        <span class="category-badge"><%= habit.category %></span>
        
        <h1 style="color: <%= habit.color || '#6366f1' %>"><%= habit.title %></h1>
        
        <p><%= habit.description || 'No description provided.' %></p>

        <div class="stats-row">
            <div>
                <span class="stat-label">Type</span>
                <span class="stat-val"><%= habit.type === 'numeric' ? 'Counter' : 'Checkbox' %></span>
            </div>
            <div>
                <span class="stat-label">Goal</span>
                <span class="stat-val"><%= habit.target_value %></span>
            </div>
            <div>
                <span class="stat-label">Created</span>
                <span class="stat-val"><%= new Date(habit.created_at).toLocaleDateString() %></span>
            </div>
        </div>

        <div class="actions">
            <a href="/habits/<%= habit.id %>/stats" class="btn btn-primary">View Charts 📈</a>
            <a href="/habits/<%= habit.id %>/edit" class="btn btn-outline">Edit Habit</a>
            
            <form action="/habits/<%= habit.id %>/delete" method="POST" style="margin:0;">
                <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure? This will delete all history for this habit.')">Delete</button>
            </form>
        </div>
    </div>
</div>

</body>
</html>