<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HabitFlow</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-body: #050505;
            --bg-card: #0f0f0f;
            --bg-hover: #1a1a1a;
            --border: #27272a;
            --text-main: #ededed;
            --text-muted: #a1a1aa;
            --primary-glow: rgba(99, 102, 241, 0.15);
            --accent-gradient: linear-gradient(135deg, #6366f1, #a855f7);
        }

        * { box-sizing: border-box; }

        body {
            background-color: var(--bg-body);
            color: var(--text-main);
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            -webkit-font-smoothing: antialiased;
        }

        .container { max-width: 900px; margin: 0 auto; padding: 40px 20px; }

        /* --- UTILITIES --- */
        .smooth-trans { transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); }
        
        .gradient-text {
            background: var(--accent-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 700;
        }

        /* --- GRID LAYOUT --- */
        .dashboard-grid {
            display: grid;
            grid-template-columns: 1fr 320px;
            gap: 24px;
            margin-bottom: 40px;
        }

        @media (max-width: 850px) {
            .dashboard-grid { grid-template-columns: 1fr; }
        }

        /* --- CARDS --- */
        .panel {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 24px; /* Super rounded */
            padding: 24px;
            position: relative;
            overflow: hidden;
        }

        /* Profile Card Styling */
        .user-header { display: flex; align-items: center; gap: 16px; margin-bottom: 20px; }
        
        .avatar {
            width: 56px; height: 56px;
            background: var(--bg-hover);
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 24px;
            border: 2px solid var(--bg-body);
            box-shadow: 0 0 0 2px var(--border);
        }

        .xp-container { margin-top: 20px; }
        .xp-bar-bg {
            height: 6px; background: #27272a; border-radius: 10px; overflow: hidden;
        }
        .xp-bar-fill {
            height: 100%;
            background: var(--accent-gradient);
            box-shadow: 0 0 10px rgba(168, 85, 247, 0.4);
            border-radius: 10px;
        }

        /* Heatmap Styling */
        .heatmap-header { display: flex; justify-content: space-between; margin-bottom: 15px; }
        .heatmap-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(12px, 1fr));
            gap: 4px;
        }
        .day-box {
            aspect-ratio: 1;
            border-radius: 4px;
            background: #1e1e22;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .day-box:hover { 
            transform: scale(1.4); 
            z-index: 10; 
            box-shadow: 0 0 10px rgba(255,255,255,0.2);
            cursor: pointer;
        }

        /* --- HABIT LIST --- */
        .section-title {
            display: flex; justify-content: space-between; align-items: center;
            margin-bottom: 16px;
        }
        .section-title h3 { font-size: 18px; font-weight: 600; margin: 0; }
        
        .btn-new {
            background: var(--text-main);
            color: var(--bg-body);
            text-decoration: none;
            padding: 8px 16px;
            border-radius: 100px;
            font-size: 13px;
            font-weight: 600;
            transition: transform 0.2s;
        }
        .btn-new:hover { transform: translateY(-2px); box-shadow: 0 4px 12px rgba(255,255,255,0.2); }

        .habit-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 18px 24px;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
        }

        .habit-card:hover {
            border-color: #52525b;
            transform: translateY(-2px);
            box-shadow: 0 10px 30px -10px rgba(0,0,0,0.5);
        }

        .habit-content { flex: 1; display: flex; flex-direction: column; gap: 4px; }
        
        .habit-title { 
            font-size: 16px; font-weight: 500; color: var(--text-main); text-decoration: none;
            display: flex; align-items: center; gap: 10px;
        }
        
        .habit-tag {
            font-size: 10px; text-transform: uppercase; letter-spacing: 0.5px;
            padding: 2px 8px; border-radius: 6px; background: var(--bg-hover); color: var(--text-muted);
        }

        /* Streak Fire Glow */
        .streak-flame {
            font-size: 12px;
            color: #fbbf24;
            text-shadow: 0 0 10px rgba(251, 191, 36, 0.4);
            background: rgba(251, 191, 36, 0.1);
            padding: 4px 8px; border-radius: 100px;
            border: 1px solid rgba(251, 191, 36, 0.2);
        }

        /* Actions */
        .habit-actions { display: flex; align-items: center; gap: 10px; }

        .btn-action {
            background: transparent;
            border: 1px solid var(--border);
            color: var(--text-muted);
            width: 36px; height: 36px;
            border-radius: 10px;
            display: flex; align-items: center; justify-content: center;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
        }
        .btn-action:hover { background: var(--bg-hover); color: var(--text-main); border-color: #52525b; }

        /* Numeric Controls */
        .numeric-wrapper {
            background: #0a0a0a;
            border-radius: 12px;
            padding: 4px;
            display: flex; align-items: center;
            border: 1px solid var(--border);
        }
        .num-btn {
            width: 28px; height: 28px; border: none; background: transparent;
            color: var(--text-muted); border-radius: 8px; cursor: pointer;
            transition: background 0.2s;
        }
        .num-btn:hover { background: var(--bg-hover); color: #fff; }
        .num-display { font-size: 13px; font-weight: 600; min-width: 24px; text-align: center; }

        /* Main Button */
        .btn-main {
            padding: 8px 20px;
            border-radius: 10px;
            font-size: 13px;
            font-weight: 500;
            cursor: pointer;
            border: none;
            transition: all 0.3s;
        }
        
        .btn-check {
            background: var(--bg-hover);
            color: var(--text-muted);
            box-shadow: inset 0 0 0 1px var(--border);
        }
        .btn-check:hover { background: #27272a; color: #fff; }

        .btn-check.completed {
            background: linear-gradient(135deg, #10b981, #059669);
            color: #fff;
            box-shadow: 0 0 15px rgba(16, 185, 129, 0.3);
        }

        /* Progress Line */
        .progress-line-bg {
            width: 100%; height: 3px; background: #27272a; border-radius: 2px; margin-top: 10px;
        }
        .progress-line-fill { height: 100%; border-radius: 2px; transition: width 0.3s; box-shadow: 0 0 8px currentColor; }

        /* Time Travel Banner */
        .time-travel {
            background: rgba(245, 158, 11, 0.1);
            border: 1px solid rgba(245, 158, 11, 0.3);
            color: #fbbf24;
            padding: 15px; border-radius: 12px;
            display: flex; justify-content: space-between; align-items: center;
            margin-bottom: 30px;
        }
        .time-travel a { color: #fbbf24; font-weight: 600; }
    </style>
</head>
<body>

<div class="container">
    <main>
        <% if (!user) { %>
            <div class="panel" style="text-align:center; padding: 80px 20px;">
                <h1 style="font-size: 40px; margin-bottom: 10px; letter-spacing: -1px;">Habit<span class="gradient-text">Flow</span></h1>
                <p style="color: var(--text-muted); margin-bottom: 40px; font-size: 18px;">Master your routine. Visualize your progress.</p>
                <div style="display:flex; gap: 15px; justify-content: center;">
                    <a href="/login" class="btn-new" style="background: transparent; border: 1px solid var(--border); color: #fff;">Log In</a>
                    <a href="/register" class="btn-new">Start Free</a>
                </div>
            </div>
        <% } else { %>

            <% if (viewDate) { %>
                <div class="time-travel smooth-trans">
                    <span>🕰️ <strong>Editing History:</strong> <%= viewDate %></span>
                    <a href="/">Return to Present →</a>
                </div>
            <% } %>

            <div class="dashboard-grid">
                <div class="panel smooth-trans">
                    <div class="heatmap-header">
                        <span style="font-size:14px; font-weight:600; color:var(--text-muted);">Consistency Graph</span>
                        <span style="font-size:12px; color:var(--text-muted);">Last 60 Days</span>
                    </div>
                    <div class="heatmap-grid">
                        <% last60Days.forEach(date => { %>
                            <% 
                                const count = history[date] || 0; 
                                let dayColor = '#1e1e22'; // Empty
                                if (count > 0) dayColor = '#064e3b'; // Darkest Green
                                if (count > 2) dayColor = '#059669'; // Mid
                                if (count > 5) dayColor = '#34d399'; // Bright
                                let opacity = count > 0 ? 1 : 0.5;
                            %>
                            <a href="/history?date=<%= date %>" class="day-box" title="<%= date %>: <%= count %> habits" 
                               style="background-color: <%= dayColor %>; opacity: <%= opacity %>"></a>
                        <% }) %>
                    </div>
                </div>

                <div class="panel smooth-trans">
                    <div class="user-header">
                        <div class="avatar"><%= user.username.charAt(0).toUpperCase() %></div>
                        <div>
                            <div style="font-size: 18px; font-weight: 700; color: #fff;"><%= user.username %></div>
                            <div style="font-size: 12px; color: var(--text-muted);">Level <%= user.level %> Architect</div>
                        </div>
                    </div>
                    
                    <div class="xp-container">
                        <div style="display:flex; justify-content:space-between; font-size:12px; color:var(--text-muted); margin-bottom:6px;">
                            <span>XP Progress</span>
                            <span style="color:#fff;"><%= user.xp %> <span style="color:#52525b;">/ <%= user.level * 100 %></span></span>
                        </div>
                        <div class="xp-bar-bg">
                            <div class="xp-bar-fill" style="width: <%= (user.xp % 100) %>%"></div>
                        </div>
                    </div>

                    <div style="margin-top: 24px; display:grid; grid-template-columns: 1fr 1fr; gap: 10px;">
                        <a href="/profile" class="btn-action" style="width: 100%; font-size: 12px; font-weight: 500;">Profile</a>
                        <a href="/leaderboard" class="btn-action" style="width: 100%; font-size: 12px; font-weight: 500;">Rankings</a>
                    </div>
                </div>
            </div>

            <div class="section-title">
                <h3>Today's Focus</h3>
                <a href="/habits/new" class="btn-new">+ New Habit</a>
            </div>

            <div class="habit-list">
                <% if (habits.length === 0) { %>
                    <div style="text-align:center; padding: 60px; color: var(--text-muted); border: 1px dashed var(--border); border-radius: 16px;">
                        No habits found. Time to build a new routine!
                    </div>
                <% } %>

                <% habits.forEach(habit => { %>
                    <% const cardColor = habit.color || '#6366f1'; %>
                    <% const querySuffix = viewDate ? '?date=' + viewDate : ''; %>

                    <div class="habit-card">
                        <div class="habit-content">
                            <div class="habit-title">
                                <a href="/habits/<%= habit.id %>"><%= habit.title %></a>
                                <% if (habit.streak > 0) { %>
                                    <span class="streak-flame">🔥 <%= habit.streak %></span>
                                <% } %>
                            </div>
                            
                            <div class="habit-meta">
                                <span class="habit-tag"><%= habit.category %></span>
                                <% if (habit.description) { %>
                                    <span style="opacity:0.6">• <%= habit.description.substring(0, 30) %>...</span>
                                <% } %>
                            </div>

                            <% if (habit.type === 'numeric') { %>
                                <% 
                                    const current = Number(habit.current_value) || 0;
                                    const target = Number(habit.target_value) || 1; 
                                    const percent = Math.min((current / target) * 100, 100);
                                %>
                                <div class="progress-line-bg">
                                    <div class="progress-line-fill" style="width: <%= percent %>%; background-color: <%= cardColor %>; color: <%= cardColor %>;"></div>
                                </div>
                            <% } %>
                        </div>

                        <div class="habit-actions">
                            <% if (habit.type === 'numeric') { %>
                                <div class="numeric-wrapper">
                                    <form action="/habits/<%= habit.id %>/decrement<%= querySuffix %>" method="POST" style="margin:0;">
                                        <button class="num-btn">-</button>
                                    </form>
                                    <span class="num-display"><%= habit.current_value %></span>
                                    <form action="/habits/<%= habit.id %>/increment<%= querySuffix %>" method="POST" style="margin:0;">
                                        <button class="num-btn">+</button>
                                    </form>
                                </div>
                            <% } else { %>
                                <form action="/habits/<%= habit.id %>/toggle<%= querySuffix %>" method="POST" style="margin:0;">
                                    <button class="btn-main btn-check <%= habit.completed ? 'completed' : '' %>">
                                        <%= habit.completed ? 'Complete' : 'Check' %>
                                    </button>
                                </form>
                            <% } %>

                            <a href="/habits/<%= habit.id %>/stats" class="btn-action" title="Stats">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 3v18h18"/><path d="M18 17V9"/><path d="M13 17V5"/><path d="M8 17v-3"/></svg>
                            </a>
                            <a href="/habits/<%= habit.id %>/edit" class="btn-action" title="Edit">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </main>
</div>

</body>
</html>