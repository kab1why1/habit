<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - HabitFlow</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Inter', sans-serif;
            background: #0a0a0a;
            min-height: 100vh;
            padding: 20px;
            color: #e5e5e5;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Header */
        .admin-header {
            margin-bottom: 32px;
            padding-bottom: 24px;
            border-bottom: 1px solid #222;
        }

        h1 {
            font-size: 32px;
            font-weight: 600;
            color: #fff;
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }

        .admin-subtitle {
            font-size: 14px;
            color: #6b6b6b;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .admin-badge {
            background: #ffa500;
            color: #0a0a0a;
            font-size: 11px;
            font-weight: 600;
            padding: 4px 8px;
            border-radius: 4px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Cards */
        .admin-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 16px;
            margin-bottom: 32px;
        }

        .card {
            background: #141414;
            padding: 32px;
            border-radius: 8px;
            border: 1px solid #222;
            transition: all 0.2s ease;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .card:hover {
            border-color: #333;
            transform: translateY(-2px);
        }

        .card-header {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .card-icon {
            width: 48px;
            height: 48px;
            background: #0a0a0a;
            border: 1px solid #333;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        .card h3 {
            font-size: 14px;
            color: #6b6b6b;
            font-weight: 500;
            text-transform: uppercase;
            margin: 0;
        }

        .card-value {
            font-size: 40px;
            font-weight: 600;
            color: #fff;
            letter-spacing: -1px;
            margin: 8px 0;
        }

        .card-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 16px;
            border-top: 1px solid #222;
        }

        .btn {
            padding: 10px 20px;
            border-radius: 6px;
            text-decoration: none;
            font-size: 13px;
            font-weight: 500;
            transition: all 0.2s ease;
            border: 1px solid #333;
            background: transparent;
            color: #e5e5e5;
            cursor: pointer;
        }

        .btn:hover {
            border-color: #555;
            background: #1a1a1a;
            transform: translateY(-1px);
        }

        @media (max-width: 768px) {
            .admin-cards {
                grid-template-columns: 1fr;
            }

            .card {
                padding: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="container">

        <!-- Header -->
        <div class="admin-header">
            <h1>Admin Dashboard</h1>
            <div class="admin-subtitle">
                <span class="admin-badge">Admin</span>
                <span>Manage users and habits</span>
            </div>
        </div>

        <!-- Cards -->
        <div class="admin-cards">

            <div class="card">
                <div class="card-header">
                    <div class="card-icon">👥</div>
                    <h3>Total Users</h3>
                </div>
                <p class="card-value"><%= usersCount %></p>
                <div class="card-footer">
                    <span></span>
                    <a href="/admin/users" class="btn">Manage Users</a>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <div class="card-icon">🎯</div>
                    <h3>Total Habits</h3>
                </div>
                <p class="card-value"><%= habitsCount %></p>
                <div class="card-footer">
                    <span></span>
                    <a href="/admin/habits" class="btn">Manage Habits</a>
                </div>
            </div>

        </div>
    </div>
</body>
</html>
