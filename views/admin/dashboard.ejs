<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - HabitFlow</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-body: #050505;
            --bg-card: #0f0f0f;
            --bg-hover: #1a1a1a;
            --border: #27272a;
            --text-main: #ededed;
            --text-muted: #a1a1aa;
            --primary: #6366f1;
            --accent-gradient: linear-gradient(135deg, #6366f1, #a855f7);
            --danger: #ef4444;
        }

        * { box-sizing: border-box; }

        body {
            background-color: var(--bg-body);
            color: var(--text-main);
            font-family: 'Inter', sans-serif;
            margin: 0; padding: 0;
            -webkit-font-smoothing: antialiased;
        }

        .container { max-width: 1200px; margin: 0 auto; padding: 40px 20px; }

        /* Header */
        .admin-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
            padding-bottom: 24px;
            border-bottom: 1px solid var(--border);
        }

        .header-left h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 800;
            letter-spacing: -1px;
            color: #fff;
        }

        .admin-tag {
            background: rgba(245, 158, 11, 0.1);
            color: #fbbf24;
            border: 1px solid rgba(245, 158, 11, 0.2);
            padding: 4px 10px;
            border-radius: 100px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-left: 12px;
            vertical-align: middle;
        }

        .subtitle {
            color: var(--text-muted);
            font-size: 14px;
            margin-top: 4px;
        }

        /* Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 24px;
            margin-bottom: 40px;
        }

        /* Card Styling */
        .stat-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 24px;
            position: relative;
            overflow: hidden;
            transition: transform 0.2s, border-color 0.2s;
        }

        .stat-card:hover {
            transform: translateY(-4px);
            border-color: #52525b;
            box-shadow: 0 20px 40px -10px rgba(0,0,0,0.5);
        }

        .card-icon-bg {
            position: absolute;
            top: -20px; right: -20px;
            font-size: 120px;
            opacity: 0.03;
            pointer-events: none;
            color: #fff;
        }

        .card-top {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 16px;
        }

        .icon-box {
            width: 40px; height: 40px;
            border-radius: 10px;
            display: flex; align-items: center; justify-content: center;
            font-size: 20px;
            background: var(--bg-hover);
            border: 1px solid var(--border);
        }

        .card-label {
            font-size: 13px;
            font-weight: 600;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .card-value {
            font-size: 42px;
            font-weight: 800;
            letter-spacing: -1.5px;
            color: #fff;
            margin: 0 0 24px 0;
            line-height: 1;
        }

        /* Card Footer Actions */
        .card-footer {
            display: flex;
            justify-content: flex-end;
            padding-top: 16px;
            border-top: 1px solid var(--bg-hover);
        }

        .btn-arrow {
            color: var(--text-main);
            text-decoration: none;
            font-size: 13px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 6px;
            transition: gap 0.2s, color 0.2s;
        }

        .btn-arrow:hover {
            gap: 10px;
            color: #6366f1;
        }

        /* Navigation Buttons */
        .nav-actions { display: flex; gap: 12px; }

        .btn-header {
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 13px;
            font-weight: 500;
            text-decoration: none;
            border: 1px solid var(--border);
            color: var(--text-muted);
            transition: all 0.2s;
        }
        .btn-header:hover {
            background: var(--bg-hover);
            color: #fff;
            border-color: #52525b;
        }
        
        .btn-logout { border-color: rgba(239, 68, 68, 0.3); color: #fca5a5; }
        .btn-logout:hover { background: rgba(239, 68, 68, 0.1); border-color: var(--danger); color: var(--danger); }
    </style>
</head>
<body>

<div class="container">

    <div class="admin-header">
        <div class="header-left">
            <h1>Dashboard <span class="admin-tag">Admin</span></h1>
            <p class="subtitle">Overview of system performance and data.</p>
        </div>
        <div class="nav-actions">
            <a href="/" class="btn-header">View Site</a>
            <a href="/admin/users" class="btn-header">Users</a>
            <a href="/admin/habits" class="btn-header">Habits</a>
            <a href="/" class="btn-header btn-logout">Exit</a>
        </div>
    </div>

    <div class="stats-grid">

        <div class="stat-card">
            <div class="card-icon-bg">👥</div>
            <div class="card-top">
                <div class="icon-box">👥</div>
                <span class="card-label">Total Users</span>
            </div>
            <h2 class="card-value"><%= usersCount %></h2>
            <div class="card-footer">
                <a href="/admin/users" class="btn-arrow">Manage Users →</a>
            </div>
        </div>

        <div class="stat-card">
            <div class="card-icon-bg">🎯</div>
            <div class="card-top">
                <div class="icon-box">🎯</div>
                <span class="card-label">Total Habits</span>
            </div>
            <h2 class="card-value"><%= habitsCount %></h2>
            <div class="card-footer">
                <a href="/admin/habits" class="btn-arrow">Manage Habits →</a>
            </div>
        </div>

    </div>

</div>

</body>
</html>