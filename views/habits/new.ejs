<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Habit - HabitFlow</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-body: #050505;
            --bg-card: #0f0f0f;
            --bg-input: #18181b;
            --border: #27272a;
            --text-main: #ededed;
            --text-muted: #a1a1aa;
            --primary: #6366f1;
            --primary-hover: #4f46e5;
            --accent-gradient: linear-gradient(135deg, #6366f1, #a855f7);
        }

        * { box-sizing: border-box; }

        body {
            background-color: var(--bg-body);
            color: var(--text-main);
            font-family: 'Inter', sans-serif;
            margin: 0; padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            width: 100%;
            max-width: 550px;
            padding: 20px;
        }

        .card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 24px;
            padding: 40px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        /* Typography */
        h2 {
            margin: 0 0 8px 0;
            font-size: 28px;
            font-weight: 800;
            letter-spacing: -0.5px;
        }
        
        .gradient-text {
            background: var(--accent-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .subtitle {
            color: var(--text-muted);
            font-size: 14px;
            margin-bottom: 32px;
            display: block;
        }

        /* Forms */
        form { display: flex; flex-direction: column; gap: 20px; }

        .form-group { display: flex; flex-direction: column; gap: 8px; }

        label {
            font-size: 13px;
            font-weight: 600;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        input, textarea, select {
            width: 100%;
            padding: 14px 16px;
            background: var(--bg-input);
            border: 1px solid var(--border);
            border-radius: 12px;
            color: var(--text-main);
            font-family: inherit;
            font-size: 15px;
            transition: all 0.2s ease;
            outline: none;
        }

        input:focus, textarea:focus, select:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.1);
            background: #27272a;
        }

        textarea { min-height: 100px; resize: vertical; }

        /* Grid for side-by-side inputs */
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        /* Tracking Section Highlight */
        .tracking-section {
            background: rgba(99, 102, 241, 0.05);
            border: 1px dashed var(--border);
            padding: 20px;
            border-radius: 16px;
            margin-top: 10px;
        }

        /* Buttons */
        .actions {
            display: flex;
            gap: 15px;
            margin-top: 10px;
        }

        .btn-primary {
            flex: 1;
            background: var(--text-main);
            color: #000;
            padding: 14px;
            border-radius: 12px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: transform 0.2s;
        }
        .btn-primary:hover {
            background: #fff;
            transform: translateY(-2px);
            box-shadow: 0 10px 20px -5px rgba(255, 255, 255, 0.15);
        }

        .btn-secondary {
            padding: 14px 24px;
            background: transparent;
            color: var(--text-muted);
            text-decoration: none;
            font-weight: 500;
            border-radius: 12px;
            border: 1px solid transparent;
            transition: all 0.2s;
            text-align: center;
        }
        .btn-secondary:hover {
            background: var(--bg-input);
            color: var(--text-main);
        }

        /* Utility */
        .hidden { display: none; }
    </style>
</head>
<body>

<div class="container">
    <div class="card">
        <h2>New <span class="gradient-text">Habit</span></h2>
        <span class="subtitle">Define a new goal to track your progress.</span>

        <form action="/habits/new" method="POST">
            
            <div class="form-group">
                <label>Title</label>
                <input type="text" name="title" required placeholder="e.g., Morning Run, Read 20 pages..." autofocus>
            </div>

            <div class="form-group">
                <label>Description (Optional)</label>
                <textarea name="description" placeholder="Why is this habit important to you?"></textarea>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label>Category</label>
                    <select name="category">
                        <option value="General">General</option>
                        <option value="Health">Health</option>
                        <option value="Work">Work</option>
                        <option value="Learning">Learning</option>
                        <option value="Fitness">Fitness</option>
                        <option value="Mindfulness">Mindfulness</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Color Tag</label>
                    <select name="color" id="colorSelect">
                        <option value="#5b4ef5">⚡ Purple</option>
                        <option value="#ff6b6b">🔥 Red</option>
                        <option value="#4caf50">🌿 Green</option>
                        <option value="#ffb74d">☀️ Orange</option>
                        <option value="#0ea5e9">💧 Blue</option>
                        <option value="#ec4899">🌸 Pink</option>
                    </select>
                </div>
            </div>

            <div class="tracking-section">
                <div class="form-group">
                    <label style="color: var(--primary);">Tracking Method</label>
                    <select name="type" id="typeSelect" onchange="toggleTarget()">
                        <option value="boolean">✅ Checkbox (Yes/No)</option>
                        <option value="numeric">🔢 Numeric Counter (e.g. 8 cups)</option>
                    </select>
                </div>

                <div id="targetWrapper" class="hidden" style="margin-top: 20px;">
                    <div class="form-group">
                        <label>Daily Target Goal</label>
                        <input type="number" name="target_value" value="1" min="1" placeholder="e.g. 10">
                        <small style="color: var(--text-muted); font-size: 12px; margin-top: 6px;">
                            How many times per day do you want to do this?
                        </small>
                    </div>
                </div>
            </div>

            <div class="actions">
                <a href="/" class="btn-secondary">Cancel</a>
                <button class="btn-primary" type="submit">Create Habit</button>
            </div>
        </form>
    </div>
</div>

<script>
    function toggleTarget() {
        const type = document.getElementById('typeSelect').value;
        const wrapper = document.getElementById('targetWrapper');
        
        if (type === 'numeric') {
            wrapper.classList.remove('hidden');
            wrapper.style.animation = "fadeIn 0.3s ease-out";
        } else {
            wrapper.classList.add('hidden');
        }
    }
</script>

</body>
</html>